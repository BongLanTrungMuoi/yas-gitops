{{- $envTag := .Values.envTag -}}
{{- $nameTag := index (splitList "-" $envTag) 0 -}}
{{- $domain := .Values.domain -}}
{{- $namespace := .Values.namespace -}}
{{- $repoURL := .Values.repoURL -}}
{{- $targetRevision := .Values.targetRevision -}}

{{- $backofficeHost := printf "backoffice-%s.%s" $envTag $domain -}}
{{- $storefrontHost := printf "storefront-%s.%s" $envTag $domain -}}
{{- $apiHost := printf "api-%s.%s" $envTag $domain -}}
{{- if not $envTag }}
  {{- $backofficeHost = printf "backoffice.%s" $domain -}}
  {{- $storefrontHost = printf "storefront.%s" $domain -}}
  {{- $apiHost = printf "api.%s" $domain -}}
  {{- $nameTag = "" -}}
{{- end }}

# --- Applications Definitions ---

# 1. yas-configuration
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "yas-configuration-%s" $nameTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/yas-configuration
    helm:
      parameters:
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: image.repository
          value: {{ printf "%s/yas-configuration" $.Values.global.image.registry }}
        - name: image.tag
          value: {{ index .Values.services "yas-configuration" "tag" | default .Values.global.image.tag }}
        # Tối ưu cho Java Startup
        - name: readinessProbe.initialDelaySeconds
          value: "180"
        - name: livenessProbe.initialDelaySeconds
          value: "240"
        - name: readinessProbe.failureThreshold
          value: "15"
        - name: livenessProbe.failureThreshold
          value: "15"
        # Trỏ sang Keycloak ở namespace infraNamespace
        - name: applicationConfig.spring.lifecycle.timeout-per-shutdown-phase
          value: "30s"
        # Trỏ Redis sang namespace infraNamespace cho các BFF
        - name: backofficeBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
        - name: storefrontBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
        # Trỏ Kafka sang namespace infraNamespace
        - name: applicationConfig.spring.kafka.bootstrap-servers
          value: "kafka-cluster-kafka-bootstrap.{{ .Values.infraNamespace }}.svc.cluster.local:9092"
        - name: applicationConfig.spring.kafka.consumer.bootstrap-servers
          value: "kafka-cluster-kafka-bootstrap.{{ .Values.infraNamespace }}.svc.cluster.local:9092"
        - name: searchApplicationConfig.elasticsearch.url
          value: "elasticsearch-es-http.{{ .Values.infraNamespace }}.svc.cluster.local"
        - name: backofficeBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
        - name: storefrontBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# 2. backoffice-bff
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "backoffice-bff-%s" $nameTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/backoffice-bff
    helm:
      parameters:
        - name: backend.ingress.host
          value: {{ $backofficeHost }}
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: backend.image.repository
          value: {{ printf "%s/yas-backoffice-bff" $.Values.global.image.registry }}
        - name: image.tag
          value: {{ index .Values.services "backoffice-bff" "tag" | default .Values.global.image.tag }}
        - name: backend.readinessProbe.initialDelaySeconds
          value: "240"
        - name: backend.livenessProbe.initialDelaySeconds
          value: "300"
        - name: backend.readinessProbe.failureThreshold
          value: "15"
        - name: backend.livenessProbe.failureThreshold
          value: "15"
        # Kết nối hạ tầng sang namespace infraNamespace
        - name: backend.databaseConnectionUrl
          value: "jdbc:postgresql://postgresql.{{ .Values.infraNamespace }}.svc.cluster.local:5432"
        - name: backofficeBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 3. backoffice-ui
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "backoffice-ui-%s" $nameTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/backoffice-ui
    helm:
      parameters:
        - name: ingress.host
          value: {{ $backofficeHost }}
        - name: ui.extraEnvs[0].name
          value: API_BASE_PATH
        - name: ui.extraEnvs[0].value
          value: "http://{{ $backofficeHost }}/api"
        - name: ui.image.repository
          value: {{ printf "%s/yas-backoffice-ui" $.Values.global.image.registry }}
        - name: image.tag
          value: {{ index .Values.services "backoffice-ui" "tag" | default .Values.global.image.tag }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 4. storefront-bff
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "storefront-bff-%s" $nameTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/storefront-bff
    helm:
      parameters:
        - name: backend.ingress.host
          value: {{ $storefrontHost }}
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: backend.image.repository
          value: {{ printf "%s/yas-storefront-bff" $.Values.global.image.registry }}
        - name: image.tag
          value: {{ index .Values.services "storefront-bff" "tag" | default .Values.global.image.tag }}
        - name: backend.readinessProbe.initialDelaySeconds
          value: "240"
        - name: backend.livenessProbe.initialDelaySeconds
          value: "300"
        - name: backend.readinessProbe.failureThreshold
          value: "15"
        - name: backend.livenessProbe.failureThreshold
          value: "15"
        # Kết nối hạ tầng sang namespace infraNamespace
        - name: backend.databaseConnectionUrl
          value: "jdbc:postgresql://postgresql.{{ .Values.infraNamespace }}.svc.cluster.local:5432"
        - name: storefrontBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 5. storefront-ui
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "storefront-ui-%s" $nameTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/storefront-ui
    helm:
      parameters:
        - name: ingress.host
          value: {{ $storefrontHost }}
        - name: ui.extraEnvs[0].name
          value: API_BASE_PATH
        - name: ui.extraEnvs[0].value
          value: "http://{{ $storefrontHost }}/api"
        - name: ui.image.repository
          value: {{ printf "%s/yas-storefront-ui" $.Values.global.image.registry }}
        - name: image.tag
          value: {{ index .Values.services "storefront-ui" "tag" | default .Values.global.image.tag }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 6. swagger-ui
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "swagger-ui-%s" $nameTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/swagger-ui
    helm:
      parameters:
        - name: ingress.host
          value: {{ $apiHost }}
        - name: image.repository
          value: {{ printf "%s/swagger-ui" $.Values.global.image.registry | default "swaggerapi" }}
        - name: image.tag
          value: {{ index .Values.services "swagger-ui" "tag" | default .Values.global.image.tag }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 7. Core Microservices
{{- range $service := .Values.coreServices }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "%s-%s" $service $nameTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/{{ $service }}
    helm:
      parameters:
        - name: backend.ingress.host
          value: {{ $apiHost }}
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: backend.image.repository
          value: {{ printf "%s/yas-%s" $.Values.global.image.registry $service }}
        - name: image.tag
          value: {{ index $.Values.services $service "tag" | default $.Values.global.image.tag }}
        # Core microservices thường nặng, cần thời gian khởi động lâu hơn
        - name: backend.readinessProbe.initialDelaySeconds
          value: "300"
        - name: backend.livenessProbe.initialDelaySeconds
          value: "360"
        - name: backend.readinessProbe.failureThreshold
          value: "20"
        - name: backend.livenessProbe.failureThreshold
          value: "20"
        # Kết nối hạ tầng sang namespace infraNamespace
        - name: backend.databaseConnectionUrl
          value: "jdbc:postgresql://postgresql.{{ $.Values.infraNamespace }}.svc.cluster.local:5432"
        # Cấu hình Kafka và Elasticsearch chéo namespace
        - name: applicationConfig.spring.kafka.bootstrap-servers
          value: "kafka-cluster-kafka-bootstrap.{{ $.Values.infraNamespace }}.svc.cluster.local:9092"
        - name: applicationConfig.spring.kafka.consumer.bootstrap-servers
          value: "kafka-cluster-kafka-bootstrap.{{ $.Values.infraNamespace }}.svc.cluster.local:9092"
        - name: searchApplicationConfig.elasticsearch.url
          value: "elasticsearch-es-http.{{ $.Values.infraNamespace }}.svc.cluster.local"
        - name: sampledataApplicationConfig.spring.datasource.product.url
          value: "jdbc:postgresql://postgresql.{{ $.Values.infraNamespace }}.svc.cluster.local:5432/product"
        - name: sampledataApplicationConfig.spring.datasource.media.url
          value: "jdbc:postgresql://postgresql.{{ $.Values.infraNamespace }}.svc.cluster.local:5432/media"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
{{- end }}
