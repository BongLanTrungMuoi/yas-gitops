{{- $envTag := .Values.envTag -}}
{{- $domain := .Values.domain -}}
{{- $namespace := .Values.namespace -}}
{{- $repoURL := .Values.repoURL -}}
{{- $targetRevision := .Values.targetRevision -}}

{{- $backofficeHost := printf "backoffice-%s.%s" $envTag $domain -}}
{{- $storefrontHost := printf "storefront-%s.%s" $envTag $domain -}}
{{- $apiHost := printf "api-%s.%s" $envTag $domain -}}
{{- if not $envTag }}
  {{- $backofficeHost = printf "backoffice.%s" $domain -}}
  {{- $storefrontHost = printf "storefront.%s" $domain -}}
  {{- $apiHost = printf "api.%s" $domain -}}
{{- end }}

# --- Applications Definitions ---

# 1. yas-configuration
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "yas-configuration-%s" $envTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/yas-configuration
    helm:
      parameters:
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: image.tag
          value: {{ index .Values.services "yas-configuration" "tag" | default .Values.global.image.tag }}
        # Tối ưu cho Java Startup
        - name: readinessProbe.initialDelaySeconds
          value: "60"
        - name: livenessProbe.initialDelaySeconds
          value: "90"
        - name: readinessProbe.failureThreshold
          value: "10"
        - name: livenessProbe.failureThreshold
          value: "10"
        # Trỏ sang Keycloak ở namespace infraNamespace
        - name: applicationConfig.spring.lifecycle.timeout-per-shutdown-phase
          value: "30s"
        # Trỏ Redis sang namespace infraNamespace cho các BFF
        - name: backofficeBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
        - name: storefrontBffExtraConfig.spring.data.redis.host
          value: "redis-master.{{ .Values.infraNamespace }}.svc.cluster.local"
        # Trỏ Kafka sang namespace infraNamespace
        - name: applicationConfig.spring.kafka.bootstrap-servers
          value: "kafka-cluster-kafka-bootstrap.{{ .Values.infraNamespace }}.svc.cluster.local:9092"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# 2. backoffice-bff
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "backoffice-bff-%s" $envTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/backoffice-bff
    helm:
      parameters:
        - name: backend.ingress.host
          value: {{ $backofficeHost }}
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: image.tag
          value: {{ index .Values.services "backoffice-bff" "tag" | default .Values.global.image.tag }}
        - name: backend.readinessProbe.initialDelaySeconds
          value: "90"
        - name: backend.livenessProbe.initialDelaySeconds
          value: "120"
        - name: backend.readinessProbe.failureThreshold
          value: "10"
        - name: backend.livenessProbe.failureThreshold
          value: "10"
        # Kết nối hạ tầng sang namespace infraNamespace
        - name: backend.databaseConnectionUrl
          value: "jdbc:postgresql://postgresql.{{ .Values.infraNamespace }}.svc.cluster.local:5432"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 3. backoffice-ui
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "backoffice-ui-%s" $envTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/backoffice-ui
    helm:
      parameters:
        - name: ingress.host
          value: {{ $backofficeHost }}
        - name: ui.extraEnvs[0].name
          value: API_BASE_PATH
        - name: ui.extraEnvs[0].value
          value: "http://{{ $backofficeHost }}/api"
        - name: image.tag
          value: {{ index .Values.services "backoffice-ui" "tag" | default .Values.global.image.tag }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 4. storefront-bff
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "storefront-bff-%s" $envTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/storefront-bff
    helm:
      parameters:
        - name: backend.ingress.host
          value: {{ $storefrontHost }}
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: image.tag
          value: {{ index .Values.services "storefront-bff" "tag" | default .Values.global.image.tag }}
        - name: backend.readinessProbe.initialDelaySeconds
          value: "90"
        - name: backend.livenessProbe.initialDelaySeconds
          value: "120"
        - name: backend.readinessProbe.failureThreshold
          value: "10"
        - name: backend.livenessProbe.failureThreshold
          value: "10"
        # Kết nối hạ tầng sang namespace infraNamespace
        - name: backend.databaseConnectionUrl
          value: "jdbc:postgresql://postgresql.{{ .Values.infraNamespace }}.svc.cluster.local:5432"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 5. storefront-ui
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "storefront-ui-%s" $envTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/storefront-ui
    helm:
      parameters:
        - name: ingress.host
          value: {{ $storefrontHost }}
        - name: ui.extraEnvs[0].name
          value: API_BASE_PATH
        - name: ui.extraEnvs[0].value
          value: "http://{{ $storefrontHost }}/api"
        - name: image.tag
          value: {{ index .Values.services "storefront-ui" "tag" | default .Values.global.image.tag }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 6. swagger-ui
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "swagger-ui-%s" $envTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/swagger-ui
    helm:
      parameters:
        - name: ingress.host
          value: {{ $apiHost }}
        - name: image.tag
          value: {{ index .Values.services "swagger-ui" "tag" | default .Values.global.image.tag }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# 7. Core Microservices
{{- range $service := .Values.coreServices }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "%s-%s" $service $envTag }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  source:
    repoURL: {{ $repoURL }}
    targetRevision: {{ $targetRevision }}
    path: k8s-cd/charts/{{ $service }}
    helm:
      parameters:
        - name: backend.ingress.host
          value: {{ $apiHost }}
        - name: global.domain
          value: {{ $domain }}
        - name: global.envTag
          value: {{ $envTag }}
        - name: image.tag
          value: {{ index $.Values.services $service "tag" | default $.Values.global.image.tag }}
        # Core microservices thường nặng, cần thời gian khởi động lâu hơn
        - name: backend.readinessProbe.initialDelaySeconds
          value: "90"
        - name: backend.livenessProbe.initialDelaySeconds
          value: "120"
        - name: backend.readinessProbe.failureThreshold
          value: "10"
        - name: backend.livenessProbe.failureThreshold
          value: "10"
        # Kết nối hạ tầng sang namespace infraNamespace
        - name: backend.databaseConnectionUrl
          value: "jdbc:postgresql://postgresql.{{ $.Values.infraNamespace }}.svc.cluster.local:5432"
        # Cấu hình Kafka nếu cần
        - name: applicationConfig.spring.kafka.bootstrap-servers
          value: "kafka-cluster-kafka-bootstrap.{{ $.Values.infraNamespace }}.svc.cluster.local:9092"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ $namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
{{- end }}
